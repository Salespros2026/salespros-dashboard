[project]
name = "salespros-dashboard-api"
version = "0.1.0"
description = "FastAPI backend dla Salespros dashboard — agreguje Meta Ads + GHL"
requires-python = ">=3.11"

[tool.ruff]
line-length = 110
target-version = "py311"
